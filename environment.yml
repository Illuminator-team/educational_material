name: illuminator-examples
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  - pip
  - pip:
    - illuminator
    - jupyterlab
    # For testing
    - pytest
    - nbclient
    - nbformat
    - pandas
    - numpy
