name: illuminator-examples
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  - pip
  - libxml2
  - libxslt
  - pandas>=3.0.3
  - numpy
  - pip:
    - illuminator
    - jupyterlab
    # For testing
    - pytest
    - nbclient
    - nbformat
